'use client';import { useState,useTransition } from 'react';import { repairCustomerLinkAction } from '@/app/(dashboard)/admin/integrity-center/actions';
export function IntegrityRepairButton({appointmentId,label}:{appointmentId:string;label:string}){const [pending,start]=useTransition();const [msg,setMsg]=useState('');return <span><button disabled={pending} onClick={()=>{if(!confirm('Confirm this sensitive repair? The action will be written to the integrity audit log.'))return;start(async()=>{const r=await repairCustomerLinkAction(appointmentId);setMsg(r.error??'Repaired');if(!r.error)location.reload()})}} className="rounded-lg bg-gold px-3 py-2 text-[10px] font-black uppercase text-black">{pending?'Repairing…':label}</button>{msg?<small className="block mt-1">{msg}</small>:null}</span>}

